# Server Configuration
PORT=3000

# Database (Postgres)
# Note: Ensure usage of these vars is enabled in app.module.ts (see Guide)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=dotech
# Set to 'false' to use Postgres, 'true' to use SQLite fallback
DB_TYPE=postgres

# Auth
JWT_SECRET=dev_secret_key_change_me
JWT_EXPIRES_IN=1d

# Stripe (Optional)
STRIPE_SECRET_KEY=sk_test_...
